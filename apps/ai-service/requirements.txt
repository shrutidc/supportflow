# Deliberately no vendor SDK. Providers are ~60 lines of httpx against the
# documented REST APIs, which keeps the provider interface honest about what it
# abstracts and avoids the churn of SDKs that rename their surface every year.
fastapi==0.141.1
uvicorn[standard]==0.41.0
httpx==0.28.1
pydantic==2.13.4

# dev
pytest==9.1.1
pytest-asyncio==1.3.0
